[pytest]
# Several suites use hyphenated filenames (test-orchestrator*.py,
# test-migrate-taxonomy-*.py). Default discovery only matches test_*.py /
# *_test.py, so those were silently skipped by `pytest tests/`. Add the
# hyphenated pattern so every suite is collected.
python_files = test_*.py *_test.py test-*.py
