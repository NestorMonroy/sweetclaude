---
spdx-license: AGPL-3.0-or-later
user-invocable: true
description: "DEPRECATED — use /sweetclaude:epics instead. Redirects automatically."
---


# Project Epics (Deprecated)

This skill has been merged into `/sweetclaude:epics`. All epic operations now live there.

Invoke `/sweetclaude:epics $ARGUMENTS` instead.

If the user invoked this skill directly, tell them:

> "The `project-epics` skill has been merged into `epics`. Redirecting now."

Then invoke `sweetclaude:epics` with the same arguments.
